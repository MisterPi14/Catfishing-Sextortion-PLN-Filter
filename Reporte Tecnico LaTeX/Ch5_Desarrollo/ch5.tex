\chapter{Método de Desarrollo}

%2025-09-11: considerar el uso de oara la propuesta de solucion cuando se hable de la infraestructura de aws https://aws.amazon.com/es/architecture/well-architected/
%07-06-2025: se tiene que justificar por que el uso de python
%07-06-2025: se tiene que justificar el por que ocupar un web app, en funcion de la diversidad de os para movil y lo complejo asi como las metricas de incidencia en Mexico.

%07-06-2025 00:45:04 : Justificar la decisión de desarrollar una aplicación web en lugar de una nativa dentro del capítulo de "Desarrollo"7777. La justificación debe mencionar la compatibilidad con múltiples sistemas operativos como Android, iOS y HarmonyOS8.

%2025-09-03: para justificar al proveedor de nube seguramente AWS, se debera hablar del aprovisionamiento como caracteristica para usar las funciones serverless añadir a marco teorico "https://www.ibm.com/mx-es/think/topics/provisioning"

% Datos para justificar el uso de AWS:
% https://holori.com/cloud-market-2022/ (imagen de la comparativa entre cloud providers)
% https://getdeploying.com/datacenters-in-mexico
% https://aws.amazon.com/es/local/mexico/


\section{Descripción del desarrollo del módulo o parte 1}
Se describe detalladamente cada módulo o parte del proyecto, se sugiere usar diagramas o esquemas, pseudocódigo y tal vez fragmentos de código. No agregar códigos completos.
\subsection{Pueden usarse subsecciones}
\subsection{Pueden usarse subsecciones}

\section{Descripción del desarrollo del módulo o parte 2}
Se describe detalladamente cada módulo o parte del proyecto, se sugiere usar diagramas o esquemas, pseudocódigo y tal vez fragmentos de código. No agregar códigos completos.
\subsection{Pueden usarse subsecciones}
\subsection{Pueden usarse subsecciones}

\section{Descripción del desarrollo del módulo o parte 3}
Se describe detalladamente cada módulo o parte del proyecto, se sugiere usar diagramas o esquemas, pseudocódigo y tal vez fragmentos de código. No agregar códigos completos.
\subsection{Pueden usarse subsecciones}
\subsection{Pueden usarse subsecciones}


\section{Generacion sintética de data set para refinamiento}

Para la generación de datos sintéticos se opta por un enfoque few shot para la formulación del prompt que se ocupara para
dar la instrucción a un modelo como Chat GPT o deepseek con madurez en tareas de generación de texto, se observo que
la generación sin datos puede dar lugar a datos poco realistas y poco variables por lo que en afinidad con las 8 practicas de
generación sintetica que plantea IBM \cite{SynthDataByIbm} en la sección del marco teorico se opta por few shot por sobre zero shot





\section{Propuesta de solución}

% % 2025/08/27 En caso de usar Android App (poco probable) https://ai.google.dev/edge/litert?hl=es-419

% % 2025/08/27  Links a las partes tecnologicas (preguntar si esto va o no) https://www.postgresql.org
% https://ollama.com/
% https://anaconda.org/anaconda/jupyter (este no se a usado en realidad se empleo mas VS code)
%(posiblemente si considerando que explica sus caracteristicas mismas que han de justificar su uso)

%Es parte de la seccopm del  desarrollo, aquí se debe hablar sobre el propósito de cada uno de los elementos de la arquitectura, diferenciar el almacenamiento remoto del local, el propósito de cuentas por lado del servidor y el almacenamiento de mensajers o todo lo que sea requerido para que el llm trabaje de forma local totalmente off line

%Justificar el uso del servidor local para servir como nodo central del sistema de mensajería: sobre el requerimiento de sincronicidad entre clientes esta parte va en propuesta de soluciion

%07/06/2025: Cambio arquitectonico de Android a Web mediante el uso de cliente servidor alojando el LLM en un servicio de vps gpu hosting

%2025-09-09: En la parte de propuesta de solución o enfoque de solución hablar sobre la calidad de la hipótesis la cuál refleja la necesidad de contar con modelos ligeros, mencionar que esto aminora la necesidad de hardware costoso o la utilización de apis para modelos con grandes capacidades contrastar con los resultados obtenidos a lo largo de las pruebas con los modelos e identificar los casos como mayores al 60% en la identificación "Aviola dice que es suficiente", falta justificar el por qué si


\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{imagenes/desarrollo/propuestaSolucion/diagramaArquitectonico.pdf}
    \caption{Diagrama arquitectónico: Arquitectura de Servicios AWS de la Aplicación de mensajería}
    \label{fig:diagrama-arquitectonico}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{imagenes/desarrollo/propuestaSolucion/diagramaComponentes.pdf}
    \caption{Diagrama de Componentes: infraestructura AWS para Aplicación de mensajería con aoporte para modelo largo de lenguaje}
    \label{fig:diagrama-componentes}
\end{figure}

\subsection{Arquitectura general del sistema (explicación)}

%2025/11/10: Esta seccion esta hecha con AI, revision y redaccion humanizada pendientes

La arquitectura propuesta representa una solución híbrida cloud-local diseñada para implementar un sistema de mensajería instantánea con capacidades de detección de amenazas en tiempo real. Este MVP (Producto Mínimo Viable) combina la escalabilidad y confiabilidad de los servicios administrados de AWS con el procesamiento local de un modelo de lenguaje (LLM) para análisis de contenido, logrando un equilibrio óptimo entre costo, rendimiento y privacidad. La arquitectura está optimizada para operar dentro de la capa gratuita de AWS, haciendo uso de servicios serverless que eliminan la necesidad aprovisionamiento y permiten un escalamiento automático según la demanda. El sistema está diseñado para detectar técnicas de sextorsión y catfishing en conversaciones de chat, alertando a los usuarios de manera proactiva cuando se identifiquen patrones de riesgo, todo esto mientras mantiene la persistencia de datos y garantiza la entrega de mensajes incluso ante fallos temporales del módulo de análisis.

\subsubsection{DESCRIPCIÓN DE SERVICIOS Y SU FUNCIONALIDAD }

1. AWS AMPLIFY HOSTING (Frontend)

AWS Amplify Hosting es el servicio encargado de alojar y distribuir la aplicación web del cliente. Este servicio proporciona hosting estático con integración continua, permitiendo desplegar aplicaciones React, Vue, Angular o HTML/CSS/JS simple de manera automática desde repositorios Git.

Funcionalidad: Amplify sirve los archivos estáticos del frontend (HTML, CSS, JavaScript) a través de una red de distribución de contenido (CDN) global, garantizando tiempos de carga rápidos. Además, maneja el certificado SSL/TLS automáticamente, proporcionando conexiones HTTPS seguras sin configuración adicional.

Por qué es necesario: Para un MVP, Amplify elimina la complejidad de configurar servidores web, gestionar certificados SSL y configurar pipelines de despliegue. Su capa gratuita incluye 15GB de almacenamiento y transferencia de datos, suficiente para las pruebas iniciales con 2 usuarios. La integración nativa con otros servicios de AWS (Cognito, API Gateway) simplifica significativamente el desarrollo.

2. AMAZON COGNITO USER POOL (Autenticación)

Amazon Cognito User Pool es el servicio de gestión de identidades y autenticación de usuarios que maneja todo el ciclo de vida de las cuentas de usuario, desde el registro hasta la autenticación y recuperación de contraseñas.

Funcionalidad: Cognito proporciona un sistema completo de autenticación basado en estándares OAuth 2.0 y OpenID Connect. Valida las credenciales de los usuarios (email y contraseña), genera tokens JWT (JSON Web Tokens) firmados que contienen información de identidad del usuario, y gestiona la renovación de sesiones. Los tokens JWT se utilizan para autorizar las solicitudes a API Gateway y otros servicios de AWS.

Por qué es necesario: Implementar un sistema de autenticación seguro desde cero es complejo y propenso a vulnerabilidades. Cognito ofrece una solución probada que incluye hash seguro de contraseñas (bcrypt), protección contra ataques de fuerza bruta, verificación de email, y cumplimiento con estándares de seguridad. Su capa gratuita permite hasta 50,000 usuarios activos mensuales (MAU), más que suficiente para un MVP. Además, se integra directamente con API Gateway para validar automáticamente los tokens JWT en cada solicitud.


3. API GATEWAY WEBSOCKET API (Comunicación en Tiempo Real)

API Gateway con soporte WebSocket es el servicio que gestiona las conexiones bidireccionales persistentes entre los clientes y el backend, permitiendo la comunicación en tiempo real sin necesidad de polling.

Funcionalidad: API Gateway WebSocket mantiene conexiones abiertas con cada cliente conectado, asignando un connectionId único a cada sesión. Cuando un mensaje llega, lo enruta a la función Lambda correspondiente según la acción especificada (enviar mensaje, obtener historial, etc.). También permite enviar mensajes desde el backend hacia los clientes conectados de forma asíncrona, habilitando notificaciones push instantáneas.

Por qué es necesario: Para lograr una experiencia de chat en tiempo real similar a WhatsApp o Telegram, se requiere comunicación bidireccional instantánea. Las alternativas tradicionales como HTTP polling son ineficientes y costosas. WebSocket mantiene una conexión TCP persistente que permite enviar datos en ambas direcciones sin la sobrecarga de establecer nuevas conexiones HTTP para cada mensaje. API Gateway gestiona automáticamente el escalamiento, la reconexión ante fallos, y la integración con Lambda, eliminando la necesidad de gestionar servidores WebSocket manualmente. Su capa gratuita incluye 1 millón de mensajes por mes.


4. AWS LAMBDA - ReceiveMessage (Procesamiento de Mensajes Entrantes)

Esta función Lambda es el punto de entrada para todos los mensajes nuevos enviados por los usuarios a través del chat.

Funcionalidad: Cuando un usuario envía un mensaje, API Gateway invoca esta función con los datos del mensaje. La función realiza tres operaciones críticas en secuencia: (1) Almacena el mensaje en DynamoDB con estado "pendiente" para garantizar persistencia inmediata, (2) Encola el mensaje en SQS para análisis posterior por el LLM local, y (3) Reenvía el mensaje al destinatario conectado a través de API Gateway WebSocket para entrega instantánea.

Por qué es necesario: Esta función implementa el patrón de "write-through cache" y desacoplamiento asíncrono. Al guardar primero en DynamoDB, se garantiza que ningún mensaje se pierda incluso si el sistema de análisis falla. Al encolar en SQS, se desacopla el procesamiento del LLM del flujo principal de mensajería, permitiendo que el chat funcione con baja latencia independientemente del tiempo de análisis. La arquitectura serverless de Lambda significa que solo se paga por el tiempo de ejecución real (primeros 1 millón de invocaciones gratis), sin necesidad de mantener servidores activos.


5. AWS LAMBDA - GetMessages (Recuperación de Historial)

Esta función Lambda maneja las solicitudes de los usuarios para recuperar el historial de conversaciones anteriores.

Funcionalidad: Cuando un usuario abre una conversación o solicita mensajes antiguos, esta función consulta DynamoDB usando el conversationId como clave de partición y recupera todos los mensajes ordenados por timestamp. Formatea los resultados incluyendo el contenido del mensaje, información del remitente, estado de análisis, y cualquier alerta de riesgo detectada.

Por qué es necesario: Separar la lógica de recuperación de historial en una función dedicada sigue el principio de responsabilidad única y permite optimizar las consultas a DynamoDB de forma independiente. Esta función puede implementar paginación para conversaciones largas, filtrado por fecha, y caché de resultados frecuentes sin afectar el rendimiento del envío de mensajes en tiempo real. La separación también facilita agregar funcionalidades futuras como búsqueda de mensajes o exportación de conversaciones.


6. AWS LAMBDA - NotifyUser (Sistema de Alertas)

Esta función Lambda se encarga de enviar notificaciones a los usuarios cuando el sistema de análisis detecta contenido de riesgo en los mensajes.

Funcionalidad: El Worker local invoca esta función cuando el LLM identifica patrones de catfishing o extorsión. La función consulta DynamoDB para obtener el connectionId del usuario que debe ser alertado, construye un mensaje de alerta con detalles del riesgo detectado (tipo de amenaza, nivel de confianza, fragmentos relevantes), y lo envía a través de API Gateway WebSocket directamente al cliente conectado.

Por qué es necesario: Las alertas de seguridad deben entregarse de forma inmediata y confiable. Esta función centraliza la lógica de notificaciones, permitiendo implementar diferentes canales de alerta (WebSocket, email, SMS) desde un único punto. Al ser invocada por el Worker local, mantiene el desacoplamiento entre el sistema de análisis y la infraestructura de AWS. También permite implementar lógica de negocio como evitar alertas duplicadas, escalar alertas según severidad, o registrar todas las notificaciones para auditoría.


7. AMAZON DYNAMODB (Persistencia de Datos)

DynamoDB es la base de datos NoSQL completamente administrada que almacena todos los datos del sistema: mensajes, metadatos de conversaciones, y estados de conexión.

Funcionalidad: La tabla ChatMessages utiliza un modelo de datos optimizado con conversationId como clave de partición (PK) y timestamp como clave de ordenamiento (SK). Esto permite consultas eficientes de todos los mensajes de una conversación ordenados cronológicamente. Cada registro almacena: messageId, senderId, receiverId, content (contenido del mensaje), status (pendiente/analizado), analysisResult (resultado del LLM), connectionId (para enrutamiento WebSocket), y metadatos adicionales.

Por qué es necesario: DynamoDB ofrece latencia de milisegundos de un solo dígito, escalamiento automático, y alta disponibilidad sin gestión de servidores. Para un MVP de chat, estas características son críticas: las consultas de historial deben ser rápidas, el sistema debe manejar picos de escritura cuando múltiples mensajes llegan simultáneamente, y los datos deben estar siempre disponibles. Su modelo de precios bajo demanda significa que solo se paga por las operaciones realmente ejecutadas. La capa gratuita incluye 25GB de almacenamiento y 25 unidades de capacidad de lectura/escritura, suficiente para miles de mensajes. El diseño de clave compuesta (PK + SK) permite consultas eficientes sin índices secundarios costosos.


8. AMAZON SQS - MessageAnalysisQueue (Cola de Análisis)

Amazon SQS (Simple Queue Service) es el servicio de cola de mensajes que actúa como buffer entre el sistema de chat en AWS y el Worker local que ejecuta el LLM.

Funcionalidad: Cada mensaje de chat se encola en SQS inmediatamente después de ser almacenado en DynamoDB. La cola está configurada con long-polling (WaitTimeSeconds: 20s) para reducir solicitudes vacías y costos. El Worker local consulta continuamente esta cola, procesa los mensajes con el LLM, y los elimina de la cola solo después de actualizar exitosamente DynamoDB. Si un mensaje falla 3 veces (por ejemplo, por errores del LLM), se mueve automáticamente a la Dead Letter Queue (DLQ).

Por qué es necesario: SQS es fundamental para la tolerancia a fallos del sistema. Si la computadora local con Ollama está apagada o sin conexión, los mensajes permanecen en la cola hasta por 14 días, garantizando que ningún mensaje quede sin analizar. El VisibilityTimeout de 300 segundos asegura que si el Worker falla durante el procesamiento, el mensaje vuelve a estar disponible automáticamente. La DLQ captura mensajes problemáticos para investigación manual sin bloquear el procesamiento de mensajes válidos. SQS también desacopla completamente el ritmo de envío de mensajes del ritmo de análisis del LLM, permitiendo que el chat funcione con baja latencia incluso si el análisis toma varios segundos. La capa gratuita incluye 1 millón de solicitudes mensuales.


9. AMAZON SQS - Dead Letter Queue (Gestión de Fallos)

La DLQ es una cola secundaria que recibe mensajes que no pudieron ser procesados exitosamente después de múltiples intentos.

Funcionalidad: Cuando un mensaje en MessageAnalysisQueue falla 3 veces consecutivas (configurado en maxReceiveCount), SQS lo mueve automáticamente a la DLQ. Estos mensajes quedan disponibles para inspección manual, reprocesamiento con parámetros ajustados, o análisis de patrones de fallo.

Por qué es necesario: Sin una DLQ, los mensajes problemáticos podrían bloquear la cola principal en un ciclo infinito de reintentos, consumiendo recursos y retrasando el procesamiento de mensajes válidos. La DLQ permite identificar problemas sistemáticos (por ejemplo, un tipo de contenido que causa errores en el LLM) sin perder datos ni afectar la operación normal. También proporciona visibilidad sobre la tasa de fallos del sistema, un indicador clave de salud para el MVP.


10. WORKER LOCAL + OLLAMA LLM (Análisis de Contenido)

El Worker es una aplicación Python que se ejecuta en una computadora local y actúa como puente entre AWS y el modelo de lenguaje Ollama.

Funcionalidad: El Worker ejecuta un bucle continuo que: (1) Consulta SQS con long-polling para obtener mensajes pendientes, (2) Extrae el contenido del mensaje y lo envía a la API de Ollama (localhost:11434) con un prompt especializado para detectar catfishing y extorsión, (3) Recibe la respuesta del LLM con clasificación de riesgo, etiquetas de amenaza, y nivel de confianza, (4) Actualiza el registro en DynamoDB cambiando el status a "analizado" y guardando el resultado, (5) Si se detecta riesgo alto, invoca la función Lambda NotifyUser para alertar al usuario, y (6) Elimina el mensaje de SQS solo después de confirmar todas las operaciones.

Por qué es necesario: Ejecutar el LLM localmente es un requisito del proyecto que reduce costos significativamente (evita pagar por inferencia en AWS SageMaker o Bedrock) y permite usar modelos personalizados optimizados para la detección específica de catfishing/extorsión. El Worker requiere permisos IAM para: sqs:ReceiveMessage, sqs:DeleteMessage (interactuar con la cola), dynamodb:UpdateItem (actualizar resultados), y lambda:InvokeFunction (enviar alertas). Ollama con modelos de 1B-4B parámetros puede ejecutarse en hardware consumer estándar (8-16GB RAM) y proporciona latencias de análisis razonables (1-5 segundos por mensaje). El diseño permite actualizar el modelo o ajustar prompts sin modificar la infraestructura de AWS.


CONCLUSIÓN

Esta arquitectura representa una solución pragmática y económica para el MVP, aprovechando servicios serverless de AWS que operan completamente dentro de la capa gratuita para volúmenes de prueba. El diseño prioriza la simplicidad operacional (sin servidores que gestionar), la tolerancia a fallos (SQS + DynamoDB garantizan que ningún mensaje se pierda), y la flexibilidad (el Worker local puede actualizarse independientemente). La separación de responsabilidades entre componentes facilita el desarrollo iterativo y el debugging, mientras que el uso de servicios administrados reduce el tiempo de implementación y los riesgos de seguridad. El sistema está preparado para escalar agregando más Workers locales si el volumen de mensajes aumenta, o migrando el LLM a AWS cuando el presupuesto lo permita.


\subsection{Pruebas de desempeño de identificación en LLMs}

se contempla el uso de técnicas de \textit{prompt engineering} para maximizar la capacidad de modelos de lenguaje (LLM) en la tarea de clasificación de mensajes sospechosos.

La etapa inicial de esta investigación contempló efectuar un conjunto de pruebas a Una colección de modelos largos del lenguaje pertenecientes al actual estado del arte de la inteligencia artificial, se incluyen a continuación:

\begin{itemize}
    \item Qwen3-VL: 4b
        \item Gemma3n: e4b, e2b
        \item Gemma3: 1b, 4b
        \item Gemma2: 2b
        \item Mistral: 7b
        \item Spam-Detect: latest
        \item Qwen3: 8b, 4b, 1.7b, 0.6b
        \item EmotionAI: latest
        \item SmallThinker: 3b
        \item Qwen2.5: 7b, 3b, 1.5b, 0.5b
        \item Phi4-mini: 3.8b
        \item Phi4-mini-reasoning: 3.8b
        \item Llama-Guard3: 8b, 1b
        \item TinyDolphin: 1.1b
        \item Llama3: 8b
        \item Llama3.2: 3b, 1b
        \item Llama3.1: 8b
        \item Llama-3-Refueled: Q4\_K\_M
        \item Opus-v1.2: Q4\_K\_M
        \item Granite3.2: 8b, 2b
        \item Granite3.3: 8b, 2b
        \item Dolphin3: 8b
        \item DeepSeek-R1: 8b, 1.5b
        \item DeepCoder: 1.5b
        \item Cogito: 3b, 8b
\end{itemize}

El propósito de estas pruebas es determinar el conjunto de modelos con mayor ponderación mediante la comparación del desempeño de esta selección de modelos en la identificación del cafishing y sextorsión contemplando refinamiento mediante técnicas de prompting bajo 3 enfoques: zero shot, one shot y few shot para determinar el modelo que se empleara en la aplicación de mensajería.

La metodología empleada para llevar a cabo este conjunto de pruebas contempló el desarrollo de Un código de Python donde se programó la lógica de programación para la Ejecución De los modelos, evaluación A través de métricas Y almacenamiento de los resultados, La presentación de los resultados se realiza en la sección “Pruebas y resultados”.

\section{Modulos del codigo}

Módulo con el prompt
Módulo de almacenamiento en json
Librerías utilizadas ollama cliente y sci kit learn
Módulos involucrados en la lógica de la evaluación: los que miden las métricas, los que manejan el orden de atención a cada modelo encontrado mediante ollama listo, 






% ...existing code...
\begin{lstlisting}[language=Python]
def saludar(nombre):
    print(f"Hola, {nombre}!")
\end{lstlisting}
% ...existing code...


\subsection{Implementación del lado del servidor}

PostgreSQL: sistema de gestión de bases de datos relacional utilizado para almacenar registros de conversación procesados, configuraciones personalizadas por usuario y resultados de inferencia.

Django: framework web de alto nivel en Python, empleado para construir las APIs que permiten la comunicación entre la aplicación de mensajería y el servidor.

Azure o Ngrok: según el entorno de despliegue, el servidor puede ser alojado bajo un esquema on-premise mediante túneles seguros con Ngrok, o bien, mediante una infraestructura como servicio (IaaS) en la nube a través de Microsoft Azure, lo que brinda escalabilidad y disponibilidad remota en entornos reales.

	extbf{Aplicación de Android}: El sistema de detección se integra en una aplicación móvil desarrollada con Android, permitiendo análisis de mensajes directamente en el dispositivo del usuario.

LiteRT (TensorFlow Lite): tecnología que permite ejecutar modelos de lenguaje previamente ajustados en dispositivos Android, sin necesidad de conexión a internet o procesamiento en la nube. Esta herramienta es clave para el cumplimiento de principios de privacidad y para brindar respuestas en tiempo real.

Android Studio: entorno de desarrollo integrado (IDE) utilizado para construir la interfaz de usuario, lógica de comunicación con el modelo y el servidor, y todas las funcionalidades necesarias para una aplicación de mensajería segura.


\subsection{Tecnologías contempladas en la propuesta}

\subsubsection{Entorno de Pruebas de desempeño de identificación en LLMs}

Ollama: para ejecutar modelos localmente en el entorno de pruebas.

Jupyter: de Anaconda para automatizar la comunicación con el modelo desde Python hacia Ollama.

\subsection{Lista de requerimientos funcionales}

\begin{itemize}
    \item El sistema debe permitir enviar y recibir mensajes
    \item El sistema debe tener inicio de sesión y creación de cuentas
    \item La clasificación debe activarse mediante el trigger “Se recibió un mensaje dentro del sistema de mensajería”.
    \item Evaluar diferentes enfoques mediante pruebas para medir el rendimiento de la inferencia del llm al realizar la tarea de clasificación del tipo de estafa y determinar el enfoque a proceder balanceando presupuesto reducido y un desempeño mínimo requerido para el requerimiento de “alertar al usuario en un tiempo pertinente para no caer en la estafa, lo más cercano al tiempo real”, los enfoques a evaluar son (refinamiento con prompt engineering o ajuste fino “fine tuning” involucrando un entrenamiento)
    \item La aplicación debe tener la opción de desactivar filtro (considerar también para las practicas, tener siempre un dispositivo que envia los mensajes sospechosos)
    \item Llevar un historial de mensajes clasificados como potencialmente peligrosos, incluyendo hora, usuario involucrado y tipo de amenaza detectada.
\end{itemize}

\subsection{Enfoques arquitectónicos para el tiempo de ejecución de inferencia del llm}

% visitar https://trail-earthquake-a0a.notion.site/07-07-2025-Enfoques-arquitectonicos-considerando-las-alternativas-22aea687ece180b687e8f8a9911c0fc5?source=copy_link para tener contexto de gpt
% https://chatgpt.com/share/686ca0a2-1eb8-800f-a390-e4d788cd355b
\begin{itemize}
    \item El modelo se ejecuta en dispositivos móviles (Edge computing).
    \item El modelo corre en un servidor on-premise (core i7 4ta gen, GPU Nvidia GeForce GTX 1060 6GB Vram, 16GB Ram del sistema, 2 terabytes de almacenamiento persistente con velocidad de 500 kB/s)
    \item El modelo corre en un Azure Container Instances o un VM Spot de azure
    \item El modelo corre en un VPS GPU hosting de wordpress
\end{itemize}


\section{Justificacion del proveedopr de IaaS}

%2025-08-31: Considerar un aspecto fuente para la justificacion del proveedopr de IaaS el que tengan data centers en cdmx o en mexico para la baja latencia, puede ser un punto fuenrte para justificar azure frente aws aunque aparentemente segun gpt sea predominante
% pagina para justificar https://getdeploying.com/



\section{Versión v1.2 de prompt para la tarea de identificación}

ROLE: You are a cybersecurity expert specialized in detecting digital fraud and malicious online behavior.
       
CONTEXT: You are analyzing text messages written in Mexico City colloquial language to identify fraudulent behavior patterns in digital communications.

DICTIONARY OF MEXICAN REGIONALISMS (These terms are common in Mexico City colloquial language and may appear in messages):

DEFINITIONS OF THE SCAMS TO IDENTIFY
1.- catfishing: Creation of a fake online identity to emotionally deceive the victim, typically requesting money, personal information, or creating emotional dependency.
key differences: focused on establishing a romantic connection to manipulate the victim later, The tone of the characteristic speech is closer to deception, manipulation through emotional ties, persuasion and the lack of naturalness with which one tries to reach the link, usually ask for money for false emergencies or other contexts.
2.-sextortion: Sexual blackmail where the attacker threatens to publish intimate content of the victim unless they comply with demands (usually money).
key differences: it involves a video, photo or any other media, The tone of the characteristic speech is closer to blackmail, coercion, coercive or directly threatening action.
3.-harmless: Normal and legitimate communication without fraudulent or malicious intentions.
key differences: it does not represent any threat, casual conversations, real plans, cordial exchanges, normal romantic relationships conversations.

ADDITIONAL EXAMPLES FOR CONTEXT (Pay close attention to these examples to understand the nuances of each category):

APPROACH: Based on the previous, classify the following message into exactly one of these three categories: catfishing, sextortion, or harmless.

LIMITS:
- Respond ONLY with the corresponding label: catfishing, sextortion, or harmless.
- DO NOT include explanations, justifications, or any additional text.
- DO NOT use quotes, periods, or special characters in your response. Just the label.

Message to analyze:


\section{Metodología de investigación en redes sociales para encontrar una aplicación de citas con potenciales estafadores}

\subsection{Metodología de investigacion para la obtención de datos reales}

La presente investigación en adición a la parte técnica presenta también una metodología que se siguió para obtener datos reales sobre estafa como lo son ejemplos de mensajería fraudulenta que busca explotar a la víctima mediante métodos afines al Catfishing y las extorsión.

Como primer paso esta parte del presente trabajo, Contempló realizar una investigación exhaustiva en foros de Internet como reddit \cite{RedditFlirtu2025}
 con el objetivo de encontrar una aplicación de mensajería donde ocurrieran frecuentemente eventualidades relacionadas a las estafas.

Los resultados de esta primera fase arrojaron que una aplicación que podríamos considerar candidata por las características que referían sus usuarios a la hora de conversar dentro de la plataforma específicamente esta plataforma se trata de una red social de citas llamada Flirt U, un bot de telegram el cual se utiliza dentro de esta plataforma.
\cite{RedditFlirtu2025}
\cite{telegramRisks}

\subsection{Metodología: Perfil controlado de observación}

Esta tarea involucró al realizar una cuenta señuelo para poder identificar y contactar con posibles estafadores dentro de la aplicación de citas para este proceso se utilizó un proyecto de internet nombrado para realizar una identidad falsa y sintética en conformidad con la ética profesional del presente trabajo.

El objetivo de esta parte puntualmente fue determinar cuáles eran los patrones clave de cada tipo de estafa a bordado en este caso cafishing y la sextorsión así como el estilo del lenguaje y la forma de la comunicación que se suele llevar a cabo por parte de los estafadores por un lado para la generación sintética de los datos y para la obtención principalmente de datos reales.

La recolección de datos se remitió exclusivamente a medios de texto, no contemplando imagen.

\subsection{Hallazgoz clave de la investigacion controlada en flirtu}

De los atacantes
* Se encontro que los estafadores de manera contraintuitiva con parte de los datos sinteticos, suelen pedir con frecuencia realizar videollamadas y efectivamente se registraron casos en los cuales del lado del atacante se visualizaban videos "posiblemente falsos con el fin de otorgar confianza en los estafadores"

\subsubsection{Características sobre el catfishing}

\begin{itemize}
    \item Crear historias difíciles para generar empatía en la víctima
    \item Establecer una comunicación que parezca genuina con fin de conectar con la víctima y establecer un lazo de confianza que posteriormente puedan explotar para obtener beneficios
    \item Las solicitudes que efectúan a las víctimas pueden tener diferentes técnicas psicológicas
    \item Solicitar tarjetas de pago, dinero o fotos
    \item Algunos perfiles aprovechaban la conexión emocional para reclutar participantes en grupos de inversión en criptomonedas, cuyas motivaciones no son identificadas en la investigación
    \item Constante solicitudes para acordar realizar una videollamada de carater intimo, mayormente con el objetivo de recabar imagenes privadas con fines de sextorsion y para utilizar posteriormente en el ejercicio de catfishing con otras victimas.
    %2025/11/10: evaluar posibilidad de citar la entrevista que se realizo con una persona afectada
\end{itemize}

\subsubsection{Características de la sextorsión} 
\begin{itemize}
    \item Solicitan perfil de redes sociales personales con propósitos como:
        \begin{itemize}
            \item Conocer detalles personales de la víctima
            \item Potencialmente acceder a la red de contactos de la persona para después chantajear tras recibir material explícito
        \end{itemize}
\end{itemize}






\subsection{Recolección y etiquetado de datos}

% 2025/11/10: poner aqui capturas del proceso y unos cuentos ejemplos, en la presentacion tambien iran
